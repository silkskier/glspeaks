# glspeaks
Repository under construction

### Recommended update/installation command:
```bash
sudo wget https://github.com/silkskier/glspeaks/releases/download/v1.0.2-alpha/glspeaks -O /usr/bin/glspeaks && sudo chmod +x /usr/bin/glspeaks
```

